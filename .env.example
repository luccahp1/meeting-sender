# URL of your main computer's receiver server
# Use the local network IP of your desktop, e.g. 192.168.1.x
RECEIVER_URL=http://192.168.1.100:5002

# Shared secret — must match the receiver's AUTH_TOKEN
AUTH_TOKEN=changeme-use-something-long-and-random

# Port this sender UI runs on
PORT=5001
